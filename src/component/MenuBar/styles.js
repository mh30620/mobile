import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    
    bar:{
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center',
        marginHorizontal: 5,
    },
});
